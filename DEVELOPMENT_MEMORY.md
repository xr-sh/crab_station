# Development Memory

## 2026-05-10

### Platform Package Module

- Continued the platform package implementation.
- Confirmed the current business rule: a package itself has no category; each selected platform spec carries its own category.
- Repaired the platform package list page after interrupted edits.
- Reworked the platform package modal to use a three-level cascader: category -> platform spec -> quantity.
- Kept the package name auto-generation rule as: spec name + category + quantity, with multiple selections joined by comma.
- Removed package-level category use from the frontend package editor and backend DTO response.
- Kept package cost display as pending calculation.
- Restored the main menu route entry under spec management: platform package -> `/platform-package`.
- Removed temporary menu click debug logs from the layout.

### Verification

- Frontend: `npm run build` passed.
- Backend: not verified locally because `mvn` is not available in the current shell. User will compile in IDEA.

### Notes

- `frontend/dist` was generated by the build and then restored so build output is not mixed into source changes.
- Backend still stores a nullable `category` column on `PlatformPackage` for compatibility, but service writes `null` and DTO no longer exposes it.

## 2026-05-10 Follow-up

### Platform Package Data Constraint Fix

- Investigated `DATA_INTEGRITY_ERROR` when creating a platform package.
- Most likely cause: existing database schema still has a non-null constraint on `platform_packages.category`, while package category is no longer a business field.
- Changed backend save logic to persist an empty string in `PlatformPackage.category` for database compatibility while keeping it hidden from DTO/API usage.
- Explicitly sets `sortNo` on package items and price rules to avoid old schema non-null/default issues.
- Added server-side logging for `DataIntegrityViolationException` so future constraint errors show the concrete database cause in backend logs.

### Verification

- Backend compile still not run locally because `mvn` is unavailable in this shell.

## 2026-05-10 Platform Package List/Delete Follow-up

### Changes

- Fixed platform package DTO conversion so `items` is populated from the entity association instead of querying by `platform_package_id`.
- This avoids the known UUID binding mismatch pattern that previously caused related records to appear empty.
- Changed platform package delete to clear child `items` and `priceRules` before deleting the parent package.
- Removed the frontend table column for quantity rules.
- Repaired the platform package list and modal text after mojibake reappeared in those files.

### Verification

- Frontend: `npm run build` passed.
- Backend: not compiled locally because `mvn` is unavailable in this shell.

## 2026-05-10 Platform Package Delete Fix

### Changes

- Fixed platform package delete failure caused by Hibernate entity delete not matching the existing database UUID representation.
- Replaced `JpaRepository.delete(entity)` for platform packages with native deletes.
- Deletes package price rules and package items by package name subquery before deleting the package row by name.
- This follows the same workaround pattern already used for platform spec delete in this project.

### Verification

- Source inspection passed.
- Backend compile still not run locally because `mvn` is unavailable in this shell.

## 2026-05-10 Spec Table Rebuild Cleanup

### Context

- User dropped platform/spec related tables and chose the broader rebuild path including purchase spec tables.
- With old table definitions gone, temporary UUID compatibility workarounds should no longer remain in core spec flows.

### Changes

- Restored platform package service to normal JPA `findById`, `save`, and `delete` paths.
- Removed native package delete-by-name methods from `PlatformPackageRepository`.
- Made `PlatformPackage.category` explicitly nullable because packages have no business category.
- Restored platform spec service to normal JPA `findById`, `save`, and `delete`.
- Removed native platform spec update/delete-by-name methods.
- Restored purchase spec service to normal JPA `findById`, `save`, and `delete`.
- Removed native purchase spec update/delete-by-name methods.
- Restored specification mapping service to normal JPA `findById`, `save`, `delete`, and repository duplicate checks.
- Repaired category validation text for `公` / `母`.

### Verification

- Frontend: `npm run build` passed.
- Backend: not compiled locally because `mvn` is unavailable in this shell; user should compile in IDEA after Hibernate recreates the dropped tables.

## 2026-05-10 Platform Package Price

### Changes

- Added package-level `price` to `PlatformPackage`.
- Added `price` to platform package create/update requests and DTO response.
- Save/update now persists package price and mirrors it into generated package price rules as `salePrice`.
- Added frontend API typing for package price.
- Added price input to the platform package modal.
- Added price column to the platform package list.
- Repaired mojibake that reappeared in platform package frontend files and backend service while making this change.

### Verification

- Frontend: `npm run build` passed.
- Backend: not compiled locally because `mvn` is unavailable in this shell.

## 2026-05-10 Specification Mapping Create Fix

### Changes

- Fixed specification mapping modal category options to submit real `公` / `母` values instead of mojibake.
- Rebuilt specification mapping list page and modal text to remove broken strings.
- Rebuilt `CreateSpecificationMappingRequest` validation messages.
- Rebuilt `SpecificationMappingService` messages and added request logging for category, purchase spec id, and platform spec id.
- Added warning logs that print existing spec IDs when a submitted purchase/platform spec ID is not found.
- Rebuilt `SpecificationMappingController` response messages.

### Verification

- Frontend: `npm run build` passed.
- Backend: not compiled locally because `mvn` is unavailable in this shell.

## 2026-05-10 Specification Mapping UUID Fallback

### Context

- User log showed `purchaseSpecId` and `platformSpecId` were submitted correctly.
- `findById` returned empty, while `findAll()` contained the exact same UUID values.
- This confirms the UUID parameter binding mismatch still exists in the current runtime.

### Changes

- `SpecificationMappingService` now falls back to `findAll()` UUID comparison when `findById` misses purchase/platform specs.
- Mapping lookup by id also falls back to `findAll()` comparison.
- Duplicate mapping checks no longer rely on nested UUID derived queries; they compare loaded entity IDs in Java.

### Verification

- Source inspection passed.
- Backend compile still not run locally because `mvn` is unavailable in this shell.

## 2026-05-10 Round Summary

### Business Decisions

- Platform packages have no package-level business category.
- Package category selection comes from each selected platform spec.
- Package selection remains a three-level cascade: category -> platform spec -> quantity.
- Package name generation remains: spec name + category + quantity, with multiple selections joined by comma.
- Package cost calculation is still pending.
- Package price is now a package-level field.

### Backend Changes

- Added platform package backend module:
  - `PlatformPackage`
  - `PlatformPackageItem`
  - `PlatformPackagePriceRule`
  - platform package DTOs
  - repositories
  - controller
  - service
- Added active platform spec endpoint used by package selection.
- Added `price` to `PlatformPackage`, create/update requests, and DTO response.
- Package save now persists `price` and mirrors it into generated price rules as `salePrice`.
- Package `category` is nullable and no longer exposed as a business field.
- Cleaned platform package service after old-table compatibility workarounds were no longer needed.
- Restored platform spec, purchase spec, and specification mapping services toward normal JPA behavior after the user dropped and rebuilt related tables.
- Fixed specification mapping create issue where submitted IDs existed in `findAll()` but missed through `findById`.
- Added UUID fallback logic in `SpecificationMappingService` for:
  - purchase spec lookup
  - platform spec lookup
  - mapping lookup
  - duplicate mapping checks
- Added diagnostic logs for specification mapping creation and missing spec IDs.
- Added data-integrity exception logging in `GlobalExceptionHandler`.

### Frontend Changes

- Added platform package API client and types.
- Added `/platform-package` route and menu entry under spec management.
- Built platform package list page.
- Built platform package modal with:
  - category/spec/quantity cascader
  - status
  - remark
  - auto-generated package name
  - package price input
- Moved the platform package price field to the end of the add/edit modal form.
- Added package price column in the platform package table.
- Removed the package quantity-rules column from the table.
- Fixed repeated mojibake in platform package page/modal.
- Rebuilt specification mapping page and modal text.
- Fixed mapping modal category values to submit real `公` / `母` instead of mojibake.

### Database / Runtime Notes

- User dropped platform/spec related tables and chose the broader rebuild path including purchase spec tables.
- Even after rebuild, runtime logs showed UUID `findById` can still miss records that are present in `findAll()`.
- Because of that, specification mapping currently keeps targeted fallback logic for UUID lookup.
- Backend still needs validation in IDEA because this shell has no `mvn`.

### Verification

- Frontend `npm run build` passed after the platform package changes.
- Frontend `npm run build` passed after the platform package price change.
- Frontend `npm run build` passed after the specification mapping page/modal fix.
- Backend compile was not run locally because Maven is unavailable in the current shell.

- Fixed platform package creation to fall back to `findAll()` when loading platform specs by UUID.
- Added `specCount` to platform package responses and changed the list spec quantity column to display selected spec-combination count.
- Fixed platform package `specCount` to use submitted selection count on save and item-table count on query instead of relying on the possibly unloaded `items` collection.
- Changed platform package `specCount` to a persisted package-level field written directly from `selections.size()` on create/update; database needs `platform_packages.spec_count`.
- Reviewed and cleaned platform package `specCount` fix: removed temporary count-based DTO path, kept `specCount` as the single persisted source written from `selections.size()`, and cleaned corrupted service messages.
- Fixed platform package delete/update/detail lookup to fall back to `findAll()` UUID comparison when repository `findById` misses an existing package.
- Fixed platform package delete stale-state error by deleting package child rows and package rows with native string-id delete queries instead of `delete(entity)`.
- Added diagnostic logs for platform package delete path id, native existence count, and existing package ids when delete target is not found.
- Platform package delete logs showed native string-id lookup missed existing IDs; removed native delete path and now delete resolves entity through the UUID fallback, clears child collections, flushes orphan removal, then deletes the package.
- Fixed platform package stale-state delete by avoiding `delete(entity)` and using native UUID-compatible deletes with `id = :uuid OR id = UNHEX(REPLACE(:uuid, '-', ''))` for package child rows and package rows.
- Platform package delete now uses JPQL bulk deletes with UUID parameters for child rows and package rows after native string/binary UUID matching still returned zero affected rows.
- Added detailed platform package delete diagnostics: entity snapshot, collection sizes, findAll matches, derived-query child counts, bulk delete affected rows, and post-delete matches.
- Platform package delete diagnostics showed Java findAll ID matching works while UUID parameter queries affect zero rows; added name+createdAt native fallback deletes for package child rows and package rows.
- Fixed platform package edit modal display: edit now loads package detail before opening, DTO conversion falls back to findAll-matched child rows, and the modal preserves existing name until selections can rebuild it.
- Hardened platform package DTO conversion so list/detail responses do not fail when a package item references a platform spec that cannot be loaded through the current UUID mapping.
- Added platform package item snapshot fields `platform_spec_name` and `platform_spec_category`; new/updated packages store selected spec name/category on item rows so DTO display no longer depends on unstable platform spec association loading.
- Added short-term platform package update workaround for UUID stale-state errors: update now validates selections, deletes the old package through the existing fallback delete path, and creates a replacement package with the new values. This changes the package id until UUID mapping is fixed.
- Rebuilt PlatformPackageService for the post-recreation schema: removed legacy UUID delete/update fallbacks, kept item snapshot fields, and returned to a clean create/update/delete path against the recreated tables.
- UUID schema alignment was tightened after Hibernate still tried to migrate UUID columns to `binary(36)` on startup. Added `hibernate.type.preferred_uuid_jdbc_type=CHAR` and explicit `@JdbcTypeCode(SqlTypes.CHAR)` on all UUID primary keys plus `PurchaseSpecPriceHistory.changedBy` so Hibernate stays aligned with the recreated `CHAR(36)` tables.
- Backend compile was not run in this shell because `mvn` is unavailable here; the user will verify locally.

## 2026-05-11 UUID Repair and Platform Package Regression Pass

### Summary

- The UUID mapping issue was traced to Hibernate still inferring `binary(36)` for UUID columns after the tables were recreated as `CHAR(36)`.
- The fix was applied at both the global and entity level:
  - `hibernate.type.preferred_uuid_jdbc_type=CHAR`
  - explicit `@JdbcTypeCode(SqlTypes.CHAR)` on UUID primary keys
  - explicit `@JdbcTypeCode(SqlTypes.CHAR)` on `PurchaseSpecPriceHistory.changedBy`
- After this change, backend compilation passed in the user's local environment.

### Validation

- Platform package create, edit, delete, list, and detail flows were tested by the user and confirmed working.
- The `specCount` field now correctly reflects the number of selected spec combinations.
- Package item snapshots now preserve platform spec name/category instead of depending on unstable live association loading.

### Outcome

- The UUID-related schema mismatch is resolved for the current CHAR-based test schema.
- The platform package module is stable on the main user-facing flows.

## 2026-05-11 Platform Package Spec Count Display

- Temporarily hid the platform package table's spec count column in the frontend.
- Kept the `specCount` field in frontend API types and backend data model/responses so the field remains available.

## 2026-05-11 Spec Range Validation

- Added create/update validation for purchase specs and platform specs so spec ranges must use `数字-数字`, for example `2.3-2.6`.
- Added backend service validation to trim ranges and require the left value to be smaller than the right value.
- Added frontend modal validation for purchase spec and platform spec forms with the same format and ordering rules.

## 2026-05-11 Platform Package Cost Calculation

- Implemented first-phase platform package cost calculation on save.
- Platform package save now resolves each selected platform spec through enabled specification mapping to an enabled purchase spec.
- Purchase spec unit cost is calculated as `range midpoint * purchase price / 10`.
- Each package selection line stores `unit cost * qty` in `PlatformPackagePriceRule.costPrice`.
- Platform package list now displays total cost by summing returned price rule costs.
- Platform package edit modal now displays saved line costs; live pre-save preview is deferred to the next phase.

## 2026-05-11 Platform Package Fixed Cost Config

- Added a small system config backend for key/value operational settings.
- Added platform package fixed cost config API at `/api/system-configs/platform-package-cost`.
- Added `fixedCost` and `totalCost` to platform package DTO responses.
- Platform package total cost now combines saved purchase cost lines with the configured fixed cost.
- Added a "固定成本" button on the platform package management page to configure labor/packaging fixed cost.
- Added remark support to the platform package fixed cost config using `system_configs.remark`.
- Split the platform package table cost display into purchase cost and total cost columns.
- Added a platform package profit column calculated as sale price minus total cost, colored green for non-negative profit and red for loss.

## 2026-05-23 Platform Package Cost Refresh

- Added automatic platform package cost refresh after a purchase spec price change.
- `PurchaseSpecService.updatePurchaseSpec` now triggers `PlatformPackageService.refreshCostsByPurchaseSpec` when the submitted price actually changes.
- The refresh finds enabled specification mappings for the changed purchase spec, locates package lines using the mapped platform specs, and recalculates AUTO price-rule costs with the existing midpoint formula.
- Manual cost rules are preserved by skipping rules with `costMode = MANUAL`.

## 2026-05-23 Purchase Unit Display

- Changed purchase management weight display from kg to jin.
- Changed purchase item unit price display from yuan/kg to yuan/jin.
- This is a display/entry-unit wording change only; existing `weight` and `unitPrice` fields are now treated as jin and yuan/jin values.

## 2026-05-24 Finance Initial Balance

- Set the finance management balance statistic to start from 10000.
- The displayed balance now calculates as `10000 + income - expense`.
