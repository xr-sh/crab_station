# crab_station 需求文档

更新时间：2026-05-03

## 1. 项目定位

`crab_station` 是一个前后端分离的内部运营管理后台，服务于进货、规格、快递、财务和平台配置等经营管理场景。

系统当前的核心不是单纯认证，而是围绕以下业务形成内部工作台：

- 进货记录与进货明细管理。
- 进货规格、平台规格和规格映射管理。
- 快递 Excel 动态导入与分析。
- 财务收支流水记录。
- 用户、权限、API 配置和定时任务配置管理。

从业务形态推断，本项目面向水产、蟹类或类似商品经营场景。采购侧关注供应商、规格、重量、单价和金额；平台侧关注外部销售平台规格；快递侧关注物流导入、时效、运费、地址和来源文件；财务侧关注基础收支流水。

## 2. 用户与权限需求

### 2.1 登录认证

系统需要用户登录后才能访问业务页面。

当前认证需求包括：

- 用户名密码登录。
- 用户注册。
- 退出登录。
- 获取当前登录用户信息。
- JWT Bearer Token 鉴权。
- 登录状态前端持久化。
- 401 未授权时清理登录状态并跳转登录页。

### 2.2 用户管理

系统需要管理员维护用户账号。

用户字段包括：

- 用户名。
- 密码。
- 邮箱。
- 手机号。
- 头像。
- 状态。
- 角色。
- 最后登录时间。
- 创建时间。
- 更新时间。

用户状态需求：

- `status=1`：启用，允许登录。
- `status=0`：禁用，不允许登录。

角色需求：

- `ADMIN`：管理员。
- `USER`：普通用户。

管理员能力：

- 分页查询用户。
- 按关键字搜索用户。
- 创建用户。
- 编辑用户资料、状态、角色和密码。
- 删除用户。
- 更新用户状态。

约束需求：

- 首个注册用户应成为管理员。
- 后续注册用户默认为普通用户。
- 禁止删除当前登录用户。
- 禁止删除、禁用或降级最后一个活跃管理员。
- 用户名唯一。
- 邮箱唯一。
- 手机号唯一。

## 3. 财务管理需求

财务模块用于记录经营中的收入和支出流水。

财务记录字段包括：

- 记录日期。
- 金额。
- 类型。
- 备注。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询财务记录。
- 按类型筛选。
- 按记录日期范围筛选。
- 创建财务记录。
- 编辑财务记录。
- 删除财务记录。

业务规则：

- 金额必须大于 0。
- 记录日期不能为空。
- 类型不能为空。

后续统计需求：

- 按筛选条件统计收入总额。
- 按筛选条件统计支出总额。
- 按筛选条件统计净收入。
- 页面翻页不应影响统计结果。

## 4. 进货管理需求

进货模块用于记录采购主单和明细。

### 4.1 进货主记录

主记录字段包括：

- 进货日期。
- 供应商。
- 总重量。
- 总金额。
- 备注。
- 明细列表。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询进货记录。
- 按供应商筛选。
- 按进货日期范围筛选。
- 创建进货记录。
- 编辑进货记录。
- 删除进货记录。
- 查看进货详情。

### 4.2 进货明细

明细字段包括：

- 进货规格。
- 重量。
- 单价。
- 金额。

业务规则：

- 创建进货记录时必须至少有一条明细。
- 明细重量必须大于 0。
- 明细单价必须大于 0。
- 明细金额由后端按 `weight * unitPrice` 计算。
- 主记录总重量由所有明细重量汇总。
- 主记录总金额由所有明细金额汇总。

后续统计需求：

- 按筛选条件统计采购总重量。
- 按筛选条件统计采购总金额。
- 按规格统计采购重量和金额。
- 页面翻页不应影响统计结果。

## 5. 规格管理需求

规格管理包括进货规格、平台规格和规格映射。

### 5.1 进货规格

进货规格表示内部采购使用的规格。

字段包括：

- 规格名称。
- 状态。
- 备注。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询进货规格。
- 按名称筛选。
- 按状态筛选。
- 查询所有启用的进货规格。
- 创建进货规格。
- 编辑进货规格。
- 删除进货规格。

业务规则：

- 规格名称唯一。
- 被进货明细引用的进货规格不能删除。
- 被规格映射引用的进货规格不能删除。

### 5.2 平台规格

平台规格表示外部平台使用的商品规格。

字段包括：

- 规格名称。
- 状态。
- 备注。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询平台规格。
- 按名称筛选。
- 按状态筛选。
- 查询所有启用的平台规格。
- 创建平台规格。
- 编辑平台规格。
- 删除平台规格。

业务规则：

- 规格名称唯一。
- 被规格映射引用的平台规格不能删除。

### 5.3 规格映射

规格映射用于建立内部进货规格和外部平台规格之间的对应关系。

字段包括：

- 进货规格。
- 平台规格。
- 状态。
- 备注。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询规格映射。
- 按进货规格名称筛选。
- 按平台规格名称筛选。
- 按状态筛选。
- 创建规格映射。
- 编辑规格映射。
- 删除规格映射。

业务规则：

- 同一进货规格和同一平台规格的组合只能存在一条映射。
- 映射关系是未来平台对接、订单处理、库存转换或数据同步的基础资料。

## 6. 快递分析需求

快递分析是当前系统最核心的业务模块之一，用于导入并分析不同快递平台导出的 Excel 文件。

### 6.1 Excel 导入

导入需求：

- 支持一次上传多个 Excel 文件。
- 支持单文件多 Sheet。
- 支持有表头和无表头两种模式。
- 支持动态读取 Excel 列。
- 支持把不固定列保存为动态字段。
- 支持记录来源文件名。
- 支持记录 Sheet 名。
- 支持记录 Excel 原始行号。
- 支持记录导入时间。
- 支持按快递类别导入，例如顺丰、京东。

导入结果需要返回：

- 快递类别。
- 文件名。
- 总处理行数。
- 成功行数。
- 失败行数。
- 识别出的列名。
- 错误信息列表。
- 是否成功。
- 提示消息。

业务规则：

- 有表头模式下，第一行作为字段名，不应计入失败数据行。
- 空行不应入库。
- 导入时应尽量兼容日期时间、纯日期和 Excel 数字日期。

### 6.2 快递记录

快递分析记录字段包括：

- 类别。
- 来源文件名。
- Sheet 名。
- Excel 行号。
- 时效描述。
- 时效小时数。
- 动态字段。
- 导入时间。
- 创建时间。
- 更新时间。

动态字段用于保存 Excel 中所有原始业务列。不同平台或不同模板的字段可以不同。

### 6.3 时效分析

系统需要根据寄件时间和签收时间计算快递时效。

时效需求：

- 从动态字段中识别寄件时间。
- 从动态字段中识别签收时间。
- 计算签收时间与寄件时间之间的小时差。
- 生成类似“x天x小时”的时效描述。
- 支持按时效小时数排序。

寄件时间字段可能包括：

- 寄件时间。
- 寄件日期。
- 寄件。
- 发货时间。
- 发货日期。
- 发出时间。
- 发出日期。

签收时间字段可能包括：

- 签收时间。
- 签收日期。
- 签收。
- 收货时间。
- 收货日期。

### 6.4 快递查询

功能需求：

- 分页查询快递分析记录。
- 按快递类别筛选。
- 按导入时间范围筛选。
- 按收件地址筛选。
- 按寄件日期范围筛选。
- 按导入时间、创建时间、更新时间、时效、类别、文件名排序。
- 查看单条快递分析记录。

辅助查询需求：

- 获取所有动态列名。
- 获取所有已导入文件名。
- 获取所有快递类别。

删除需求：

- 删除单条记录。
- 管理员清空所有快递数据。
- 管理员按文件删除快递数据。

### 6.5 快递统计

统计需求：

- 总记录数。
- 已导入文件数。
- 动态列数量。
- 按快递类别统计记录数。
- 平均运费。

运费字段可能包括：

- 运费。
- 费用。
- 快递费。
- 物流费。
- 快递费用。
- `fee`。
- `shippingFee`。
- `expressFee`。

### 6.6 后续快递需求

后续应补充：

- 导入批次概念。
- 文件 hash 去重。
- 按批次删除。
- 标准字段抽取，例如寄件时间、签收时间、收件地址、运费。
- 基于标准字段的查询、排序和统计。

## 7. API 配置管理需求

API 配置模块用于保存外部平台接口配置。

字段包括：

- 平台名称。
- API Key。
- Secret 或 Token。
- Base URL。
- 动态配置。
- 状态。
- 备注。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询 API 配置。
- 按平台名称筛选。
- 按状态筛选。
- 查询所有启用 API 配置。
- 根据 ID 查询 API 配置。
- 根据平台名称查询 API 配置。
- 创建 API 配置。
- 编辑 API 配置。
- 删除 API 配置。

业务规则：

- 平台名称唯一。
- API 配置属于管理员功能。
- API Key 和 Secret 在响应中应脱敏。
- 动态配置用于保存平台差异化参数，例如 accessToken、refreshToken、店铺 ID、业务开关等。

需求边界：

- 当前 API 配置只是配置库。
- 当前没有真实第三方平台调用链路。
- 页面和文档不应暗示已经完成平台自动同步。

## 8. 定时任务配置需求

定时任务模块用于保存未来自动化任务的配置。

字段包括：

- 任务名称。
- 任务类型。
- Cron 表达式。
- 任务参数。
- 上次执行时间。
- 下次执行时间。
- 执行次数。
- 上次执行状态。
- 上次执行消息。
- 状态。
- 备注。
- 创建时间。
- 更新时间。

功能需求：

- 分页查询定时任务。
- 按任务名称筛选。
- 按任务类型筛选。
- 按状态筛选。
- 查询所有启用的定时任务。
- 创建定时任务。
- 编辑定时任务。
- 删除定时任务。
- 启停任务。

业务规则：

- 定时任务配置属于管理员功能。
- 任务名称唯一。

需求边界：

- 当前模块只是“任务配置管理”。
- 当前没有真实调度器、任务执行器、执行日志和 Cron 校验。
- 页面和文档不应暗示任务已经会自动执行。

## 9. 首页与导航需求

系统需要提供统一后台布局。

导航需求：

- 首页。
- 用户管理。
- 财务管理。
- 进货管理。
- 规格管理。
- 快递分析。
- API 配置。
- 定时任务。

布局需求：

- 桌面端使用侧边导航。
- 移动端使用抽屉导航。
- 顶部显示当前用户入口。
- 支持退出登录。

首页需求：

- 首页应展示关键业务概览。
- 当前首页偏占位，后续应接入真实统计数据。

## 10. 非功能需求

### 10.1 安全

安全需求包括：

- 生产环境必须使用非默认 JWT Secret。
- 密码必须加密存储。
- 高权限接口必须限制管理员访问。
- API Secret 不应明文返回。
- 后续 API Secret 应加密入库。
- 后续应评估 HttpOnly Cookie 或 CSP，降低 localStorage token 风险。

### 10.2 性能

性能需求包括：

- 分页接口必须限制最大 page size。
- 大 Excel 导入不能无限制占用内存。
- 快递统计不能长期依赖全表 JSON 扫描。
- 高频查询字段应标准化并建立索引。

### 10.3 可维护性

维护需求包括：

- 中文文案、注释和校验提示应统一编码。
- 关键业务字段名应集中维护。
- 数据库结构变更应引入迁移工具。
- 需求文档、开发计划和变更日志应分离维护。

### 10.4 可验证性

后续需要补充测试覆盖：

- 认证和权限。
- 用户管理员保护。
- 采购金额计算。
- 规格引用删除保护。
- 规格映射唯一性。
- API 配置脱敏。
- Excel 导入。
- 快递查询和统计。

